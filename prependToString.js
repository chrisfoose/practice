// prepending a string to another string

let firstPart = prompt('Enter the first part');
let secondPart = prompt('Enter the second part');

fullString = 'Here is the prepended string: ' + secondPart + firstPart;
console.log(fullString);